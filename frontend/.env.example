# Frontend Environment Configuration

# Debug Mode
# Set to 'true' to enable debug logging (console.log, console.warn, console.error)
# When disabled, no debug output will be shown
NEXT_PUBLIC_DEBUG_MODE=false

# API Base URL Configuration
# Local API URL - used when running on localhost
# Defaults to http://localhost:8000 if not set
NEXT_PUBLIC_API_BASE_URL_LOCAL=http://localhost:8000

# Ngrok API URL - used for production or when accessing from network
# This should be your ngrok tunnel URL (e.g., https://xxxx-xx-xx-xx-xx.ngrok-free.app)
# The protocol will be automatically converted to HTTPS if needed
NEXT_PUBLIC_API_BASE_URL_NGROK=

# Environment
# Set to 'production' for production builds, 'development' for development
# If not set, defaults to NODE_ENV
NEXT_PUBLIC_ENVIRONMENT=development
